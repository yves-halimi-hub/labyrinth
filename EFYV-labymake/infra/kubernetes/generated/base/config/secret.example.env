# Example secrets — local throwaway values. Real secrets are injected out-of-band
# (git-ignored repoint or the external-secrets operator in prod). NEVER commit real values.
AUTH_SECRET=change-me
INITIAL_ADMIN_EMAIL=change-me
INITIAL_ADMIN_PASSWORD=change-me
SMTP_PASSWORD=change-me
POSTGRES_PASSWORD=change-me
REDIS_PASSWORD=change-me
