# GENERATED by efyv-nodegen from node/topology.py — DO NOT EDIT.
# Change the node topology and run `efyv-nodegen gen`.
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/component: web
spec:
  selector:
    app.kubernetes.io/name: frontend
  ports:
  - name: svc
    port: 3210
    targetPort: 3210
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/component: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: frontend
  template:
    metadata:
      labels:
        app.kubernetes.io/name: frontend
        app.kubernetes.io/component: web
    spec:
      containers:
      - name: frontend
        image: "efyv/web:local"
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3210
        env:
        - name: BACKEND_HOST
          valueFrom:
            configMapKeyRef:
              name: efyv-labymake-topology
              key: BACKEND_HOST
        - name: BACKEND_PORT
          valueFrom:
            configMapKeyRef:
              name: efyv-labymake-topology
              key: BACKEND_PORT
        - name: BACKEND_BASE_URL
          value: "http://$(BACKEND_HOST):$(BACKEND_PORT)"
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: efyv-labymake-config
              key: NODE_ENV
        - name: EFYV_PLAN_PATH
          value: /etc/efyv/platform-plan.json
        - name: EFYV_PLAN_SHA256
          value: b321bc9fdd27881549deea461349aae875209a509641dfa60f6bbcbb33f780d5
        livenessProbe:
          httpGet:
            path: /
            port: 3210
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /
            port: 3210
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 6
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 1000m
            memory: 512Mi
        volumeMounts:
        - name: runtime-plan
          mountPath: /etc/efyv/platform-plan.json
          subPath: platform-plan.json
          readOnly: true
      initContainers:
      - name: wait-for-deps
        image: "busybox:1.37"
        command:
        - sh
        - "-c"
        - until nc -z backend 8000; do echo waiting for backend; sleep 2; done
      volumes:
      - name: runtime-plan
        configMap:
          name: efyv-labymake-runtime-plan
