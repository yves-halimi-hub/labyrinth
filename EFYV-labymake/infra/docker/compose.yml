# GENERATED by efyv-nodegen from node/topology.py — DO NOT EDIT.
# Change the node topology and run `efyv-nodegen gen`.
name: "${COMPOSE_PROJECT_NAME:-efyv-labymake}"
services:
  frontend:
    image: "efyv/web:${EFYV_VERSION:-0.1.0}"
    environment:
      BACKEND_BASE_URL: "http://${BACKEND_HOST}:${BACKEND_PORT}"
      NODE_ENV: "${NODE_ENV:-production}"
      EFYV_PLAN_PATH: /etc/efyv/platform-plan.json
      EFYV_PLAN_SHA256: f3d5482e61ab8ee334c823fa551c52829c8833d27b0ecd9794da82f1a8c3b964
    depends_on:
      backend:
        condition: service_healthy
    ports:
    - "${HOST_BIND:-127.0.0.1}:3210:3210"
    volumes:
    - "../../.efyv/build/platform-plan.json:/etc/efyv/platform-plan.json:ro"
    healthcheck:
      test:
      - CMD
      - wget
      - "-q"
      - "-O"
      - "-"
      - "http://127.0.0.1:3210/"
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: "${RESTART_POLICY:-unless-stopped}"
  backend:
    image: "efyv/backend:${EFYV_VERSION:-0.1.0}"
    environment:
      DATABASE_URL: "postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}"
      REDIS_URL: "redis://:${REDIS_PASSWORD}@${REDIS_HOST}:${REDIS_PORT}/0"
      COMPUTE_URL: "http://${COMPUTE_HOST}:${COMPUTE_PORT}"
      APP_ENV: "${APP_ENV:-dev}"
      LOG_LEVEL: "${LOG_LEVEL:-INFO}"
      CORS_ORIGINS: "${CORS_ORIGINS}"
      AUTH_SECRET: "${AUTH_SECRET}"
      SESSION_TTL_MINUTES: "${SESSION_TTL_MINUTES:-720}"
      COOKIE_SECURE: "${COOKIE_SECURE:-false}"
      INITIAL_ADMIN_EMAIL: "${INITIAL_ADMIN_EMAIL:-}"
      INITIAL_ADMIN_PASSWORD: "${INITIAL_ADMIN_PASSWORD:-}"
      SMTP_HOST: "${SMTP_HOST:-}"
      SMTP_PORT: "${SMTP_PORT:-587}"
      SMTP_USER: "${SMTP_USER:-}"
      SMTP_PASSWORD: "${SMTP_PASSWORD:-}"
      EFYV_PLAN_PATH: /etc/efyv/platform-plan.json
      EFYV_PLAN_SHA256: f3d5482e61ab8ee334c823fa551c52829c8833d27b0ecd9794da82f1a8c3b964
      EFYV_PROTO_DESCRIPTOR_ROOT: /etc/efyv/protobuf-descriptors
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      compute:
        condition: service_healthy
    ports:
    - "${HOST_BIND:-127.0.0.1}:8000:8000"
    volumes:
    - "../../.efyv/build/platform-plan.json:/etc/efyv/platform-plan.json:ro"
    - "../../.efyv/build/custom-services/labymake-engine/descriptor.pb:/etc/efyv/protobuf-descriptors/.efyv/build/custom-services/labymake-engine/descriptor.pb:ro"
    healthcheck:
      test:
      - CMD
      - wget
      - "-q"
      - "-O"
      - "-"
      - "http://127.0.0.1:8000/health/ready"
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: "${RESTART_POLICY:-unless-stopped}"
  compute:
    image: "efyv/compute:${EFYV_VERSION:-0.1.0}"
    environment:
      EFYV_PLAN_PATH: /etc/efyv/platform-plan.json
      EFYV_PLAN_SHA256: f3d5482e61ab8ee334c823fa551c52829c8833d27b0ecd9794da82f1a8c3b964
    volumes:
    - "../../.efyv/build/platform-plan.json:/etc/efyv/platform-plan.json:ro"
    healthcheck:
      test:
      - CMD
      - wget
      - "-q"
      - "-O"
      - "-"
      - "http://127.0.0.1:9000/health/ready"
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: "${RESTART_POLICY:-unless-stopped}"
  postgres:
    image: "efyv/postgres:${EFYV_VERSION:-0.1.0}"
    environment:
      POSTGRES_USER: "${POSTGRES_USER:-efyv}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"
      POSTGRES_DB: "${POSTGRES_DB}"
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
    - "pgdata:/var/lib/postgresql/data"
    healthcheck:
      test:
      - CMD
      - pg_isready
      - "-U"
      - "${POSTGRES_USER}"
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: "${RESTART_POLICY:-unless-stopped}"
  redis:
    image: "efyv/redis:${EFYV_VERSION:-0.1.0}"
    environment:
      REDIS_PASSWORD: "${REDIS_PASSWORD:-}"
    healthcheck:
      test:
      - CMD
      - redis-healthcheck
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: "${RESTART_POLICY:-unless-stopped}"
  labymake-engine:
    build:
      context: ../../services/labymake-engine
      dockerfile: Dockerfile
    image: "efyv-labymake-labymake-engine:${EFYV_TAG:-local}"
    environment:
      EFYV_GRPC_PORT: "50051"
    restart: "${RESTART_POLICY:-unless-stopped}"
volumes:
  pgdata: {}
