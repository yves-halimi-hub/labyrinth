# Verification Harness

[Repository](../README.md) | [Core](../Core/README.md) | [Shared backend](../../EFYV-labybackend/)

`EFYVLabyMake.Verification.csproj` is a dependency-free .NET 8 executable, not an xUnit/NUnit project. It links every `Core/**/*.cs` file from this repository and every backend `Core/**/*.cs` file from the sibling repository, enables unsafe code, and runs deterministic assertions from a partial `Program` class.

## Run

From the LabyMake repository root:

```powershell
dotnet build Tests/EFYVLabyMake.Verification.csproj --configuration Debug
dotnet run --project Tests/EFYVLabyMake.Verification.csproj --configuration Debug
```

The runner prints the total assertion and group counts and exits nonzero on the first failed invariant.

## Test files

- [`Program.cs`](Program.cs): runner, shared fixtures/helpers, baseline schema, compositing, export, preview, persistence, autosave, and live-debug checks.
- [`ModelsAndToolsTests.cs`](ModelsAndToolsTests.cs): clone isolation, randomized compositing, exact drawing reference models, animation generation, snapshots, and viewport boundaries.
- [`SchemaValidatorCommandTests.cs`](SchemaValidatorCommandTests.cs): exhaustive schema/toolbar matrices, validator issue coverage, structural-scope validation, and randomized bounded-history modeling.
- [`SessionMapPreviewTests.cs`](SessionMapPreviewTests.cs): session CRUD and gesture rollback, extreme preview timing, the structural preview-validation scope, and deterministic map modes.
- [`StorageAndExportTests.cs`](StorageAndExportTests.cs): path attack corpus, asset-bank corruption, malformed project documents, atomic cancellation, exact decoded PNG pixels (decoded through the production backend `FastPngDecoder` rather than a hand-rolled harness reader), the near-square grid atlas layout round trip, publication rollback, the `.efyvlaby` documentVersion/baseAssetType contract, the snapshot-overload atlas caps and identity rejection, and the retrying publish rollback under a locked destination.
- [`AsyncLifecycleTests.cs`](AsyncLifecycleTests.cs): adversarial live-debug/autosave state machines and session save/reload lifecycle.
- [`ToolsDeepTests.cs`](ToolsDeepTests.cs): exact reference models for the pencil thick-line, flood-fill, stamp-blit, and tile-maker wrap paths, the hitbox gesture state machine, moving-tool/generator equivalence, and a white-box MapTool RNG/sequencing model.
- [`SessionLogicDeepTests.cs`](SessionLogicDeepTests.cs): designer-session construction/selection/no-op/index tracking, hitbox gestures and generate-replace, a randomized gesture diff model with byte-exact history accounting, command internals and throwing commands, the undo/redo selection-clamp policy, the gesture rollback/replay-failure contracts, validator boundary limits, schema chains/value kinds, and asset-bank byte layout plus corruption corpus.
- [`LiveLogicDeepTests.cs`](LiveLogicDeepTests.cs): cumulative preview playback and residual/empty state machines, per-pixel viewport render and transform reference models, toolbar categories and normalization corners, walk/jitter animation reference models, the TaskDebounceScheduler contract, and LiveDebugController synchronization-context/time/failure paths.
- [`ModelsPersistenceDeepTests.cs`](ModelsPersistenceDeepTests.cs): model clone/constructor contracts with a randomized layer pixel model, snapshot composition and property capture, byte-level project document and property round trips, save guards and failure atomicity, export atlas metadata/publication, and autosave synchronization-context dispatch.
- [`PixelToolsSelectionResizeTests.cs`](PixelToolsSelectionResizeTests.cs): the item #9 tool surface — eraser true-transparency and single-command strokes, mirror-symmetry models for pencil/eraser/fill/shapes, shape-tool gesture preview and one-command commit, selection-region geometry (rect clamps plus a randomized independent even-odd lasso reference), the session floating-buffer lifecycle (lift/move/anchor as one command, cancel, clipboard reuse/isolation, transient drops on structural edits), and the ResizeCanvas nine-anchor reference model with history, guards, and a persistence round trip.
- [`AnimationWorkflowTests.cs`](AnimationWorkflowTests.cs): the item #10 animation surface — Frame.DurationMs/playback-tag model contracts with clone and snapshot fidelity, the optional `.efyvmake` timing members (round trip, legacy documents, malformed corpus), the PreviewController rewrite (mixed per-frame durations, loop ranges with intros, ping-pong sequences, exact-cycle and huge-elapsed modulo reduction, non-looping tag immunity), the undoable duration/loop-range/ping-pong session commands, the atomic cross-frame layer batches (add/remove/rename/visibility with divergent-state undo), the layer-preserving regenerate merge plus the bob/breathe and shake/hit-flash preset facades, the export flow of timing metadata into the `.efyvlaby` atlas (defaults omitted, stale ranges clamped), and the onion-skin compositor (reference blending order, falloff, edge clamping, screen-buffer overload).
- [`EffectsAuthoringFilterTests.cs`](EffectsAuthoringFilterTests.cs): the item #7 surface — the immutable `EffectDescriptor` validation gate with clone/snapshot reference sharing, the undoable session effect CRUD (add/remove/replace, cap, same-reference no-op), the optional `.efyvmake` effects section (round trip, legacy documents restoring empty, malformed corpus, over-cap, null-entry save gate), the export flow of authored effects into the `.efyvlaby` atlas (omission when empty, shared-validator agreement, null-entry export rejection), and the destructive layer filters (blur/outline/glow/color-shift as ONE sparse `FrameEditCommand` each with byte-exact undo, no-op filters recording nothing, parameter/gesture/frame guards, selection-region write masking with region survival, floating-buffer cancel-before-filter, and the ToolbarAPI filter catalog against config bounds).
- [`SubElementPipelineTests.cs`](SubElementPipelineTests.cs): the item #6 surface — SubElement pivot/default-transform contracts (center default matching the legacy stamp centering, validated setters) and the `SubElementAttachment` gate/clone/grab-box, the `.efyvsub` version-2 round trip with version-1 legacy reads plus a corrupt corpus (future version, bad pivot/flags/z-order) and the `TryResolveSubElement` resolver contract, the undoable session attachment CRUD (add/move/remove, per-frame cap, mid-gesture refusal, validator issue codes), the stamp tool's attachment gestures (place+drag as ONE command, anchor grab/reposition, element-less repositioning, cap no-op, Esc rollback) and pivot-anchored legacy bake, the `.efyvmake` attachments section (round trip, legacy documents, malformed corpus, null-entry save gate, ResizeCanvas anchor shift), and the export flow (z-ordered pivot/flip-correct flattening verified through decoded PNG pixels, unresolved-name metadata-only skip, structured `attachments` records with global frame indices, omission for attachment-free projects).
- [`PaletteColorWorkflowTests.cs`](PaletteColorWorkflowTests.cs): the item #8 color surface — Palette/RecentColorRing model contracts with a randomized nearest-metric reference (tie-break and alpha participation pinned), the optional `.efyvmake` palette section (round trip, snapshot isolation, legacy documents without the section, save-side and load-side malformed corpora), the undoable session palette CRUD + swatch selection + recent tracking, the composited eyedropper (bit-exact against `FlattenLayers` at every pixel, no history, event-per-gesture), the one-history-entry global color swap (frame/all-frames scopes, hidden layers, shared-layer de-dup, transparent swaps, floating-cancel ordering, randomized reference fuzz), and the palette-constraint gesture snap across pencil/fill with eraser/eyedropper immunity.
- [`ViewportOverlayZoomTests.cs`](ViewportOverlayZoomTests.cs): the item #31 surface — the public `SetZoom`/`SetPan`/`ResetView` viewport API (clamping, non-finite rejection, bit-identical parity with the anchored `OnScroll` step, anchor-point preservation), and the designer overlay passes against pixel-exact reference models that independently replicate the blit mapping and the `FastMemory.BlendColor` math: the core checkerboard backdrop (opaque inside-canvas output, screen-anchored cell phase, onion-composite interaction, the no-behavior-change contract for null/None settings), pixel/tile grid boundary lines (threshold gating, inactive tile context, tile-over-pixel draw order, cell-size-1 equivalence, a 120-round randomized geometry fuzz), per-key hitbox rectangles (deterministic FNV colors, degenerate/non-finite box skips, screen clipping, sub-pixel minimum line), attachment outlines pinned to the export flatten's placed bounds plus pivot-marker crosses (explicit pivot + attachment anchors, exactly-once blending, radius guards), and pass composition (current-frame-only overlay context under onion skin, floating-selection compositing, state-free re-renders, a zero-allocation steady-state guard).
- [`DirectionalAuthoringTests.cs`](DirectionalAuthoringTests.cs): the item #33 surface — the `DirectionalState` model gate and by-reference facing switches with `EFYVProject` routing (`GetFacingAnimations`, `EnumerateAllAnimations`), linked-project creation and the facing-options catalog (`CreateNewLinkedDirectionalProject`/`GetFacingOptions`), the undoable session commands (`EnableDirectionalAuthoring` with facing-property seeding and non-directional rejection, `SwitchFacing` with LIFO undo/redo across facings and the facing-property `SetProperty` routing, `GenerateMirroredFacing` with pixel/hitbox/attachment mirror reference checks and a double-mirror round trip), export-scope-only completeness validation (`DirectionalFacingIncomplete` subjects, structural checks on parked sets), the `.efyvmake` directional section (round trip, facing-property resync, legacy documents, malformed corpus, null-entry save gate), the one-export-all-facings flow (per-facing suffixed pairs with overridden `facing` properties, `ExportAllFacings` ordering with the active facing last, incomplete-project rejection) plus the directional live-debug push, and the item #33b custom-field surface (the `RegisterAssetType` custom-fields overload: field shape/defaults, the full rejection matrix over reserved/manifest/inherited/duplicate/overlong names and the count cap, plus the export flow landing custom keys in `.efyvlaby` properties for plain and linked projects).
- [`MapTilesetPipelineTests.cs`](MapTilesetPipelineTests.cs): the item #5 designer surface - TilesetSection/TilesetTile and MapSection model gates with the MapRenderer reference model (tile blits, deterministic placeholder cells, transparent blanks), the undoable session tileset CRUD (create/remove, blank and captured-from-frame tiles, rename/remove, the tile cap), the undoable map editing surface (per-cell writes with surfaced out-of-bounds, rect/flood bulk fills as one sparse entry each, Begin/Paint/End paint strokes batching to one entry with Esc cancellation and undo-auto-cancel, MapTool gestures recording tile diffs plus appended props with seeded noise determinism), the `.efyvmake` tileset/map sections (round trip, snapshot isolation, legacy documents, malformed corpus, save-side prop-cap gate), the tile-sheet export end to end (near-square tile-id-order packing verified through decoded PNG pixels, the single Tiles atlas animation, manifest read-back through FastImporter, empty-tileset refusal), and the `.efyvmap` publication round trip (path shape, FastMapImporter read-back, atomic republish).

## Adding coverage

Add methods to an external `internal static partial class Program` file, register each group in `Program.Main`, and reuse the bounded fixtures and `ManualScheduler`. Tests should use fixed seeds, temporary directories with `finally` cleanup, explicit canaries/reference models for unsafe code, and finite workloads. Avoid external packages so the harness continues to verify the exact source shared with Unity.

